# Resumes Directory

This directory is intended to store generated resumes or specific resume overrides.

Typically, the AI Orchestrator will place its final output artifacts (like `.pdf`, `.html`, `.json`, and `.md` formats of your resume) in this location. You can also place manually tailored versions of your resume here.
